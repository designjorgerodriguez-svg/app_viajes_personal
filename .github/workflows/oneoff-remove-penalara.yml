name: One-off remove Peñalara

on:
  push:
    branches: ["main"]
    paths:
      - "scripts/oneoff-remove-penalara.mjs"
      - ".github/workflows/oneoff-remove-penalara.yml"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 10
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Remove Peñalara
        run: node scripts/oneoff-remove-penalara.mjs
      - name: Validate data
        run: pnpm validate:data
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/data/trips/pais-vasco-frances.json
          git diff --cached --quiet || git commit -m "content: eliminar Pico de Peñalara"
          git push
