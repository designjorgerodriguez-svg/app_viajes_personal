import { readFile, writeFile } from 'node:fs/promises'

const detailsPath = new URL('../src/features/places/PlaceDetails.tsx', import.meta.url)
const cssPath = new URL('../src/styles/app.css', import.meta.url)

let details = await readFile(detailsPath, 'utf8')
let css = await readFile(cssPath, 'utf8')

const oldHeader = `      <header className="place-details__header">\n        <div className="place-details__title">\n          <h1>{place.name}</h1>\n          <p>\n            <span className="category-label" style={{ color: category.color }}>{category.label}</span>\n            <span aria-hidden="true"> · </span>\n            {place.locality}\n          </p>\n        </div>\n        <div className="place-details__header-actions">\n          <button\n            className="icon-button icon-button--state"\n            data-active={state.favorite}\n            onClick={onToggleFavorite}\n            type="button"\n            aria-label={state.favorite ? 'Quitar de favoritos' : 'Añadir a favoritos'}\n            aria-pressed={state.favorite}\n          >\n            <Star size={17} fill={state.favorite ? 'currentColor' : 'none'} />\n          </button>\n          <button\n            className="icon-button icon-button--state"\n            data-active={state.visited}\n            onClick={onToggleVisited}\n            type="button"\n            aria-label={state.visited ? 'Marcar como no visitado' : 'Marcar como visitado'}\n            aria-pressed={state.visited}\n          >\n            <Check size={18} strokeWidth={2.5} />\n          </button>\n          {routeActive ? (\n            <button\n              className="icon-button icon-button--route-collapse"\n              onClick={onCollapse}\n              type="button"\n              aria-label="Plegar ficha y mantener la ruta"\n            >\n              <ChevronDown size={18} />\n            </button>\n          ) : null}\n          <button className="icon-button icon-button--ghost" onClick={onClose} type="button" aria-label="Cerrar ficha">\n            <X size={19} />\n          </button>\n        </div>\n      </header>`

const newHeader = `      <header className="place-details__header">\n        <div className="place-details__title">\n          <h1>{place.name}</h1>\n          <div className="place-details__header-actions">\n            <button\n              className="icon-button icon-button--state"\n              data-active={state.favorite}\n              onClick={onToggleFavorite}\n              type="button"\n              aria-label={state.favorite ? 'Quitar de favoritos' : 'Añadir a favoritos'}\n              aria-pressed={state.favorite}\n            >\n              <Star size={17} fill={state.favorite ? 'currentColor' : 'none'} />\n            </button>\n            <button\n              className="icon-button icon-button--state"\n              data-active={state.visited}\n              onClick={onToggleVisited}\n              type="button"\n              aria-label={state.visited ? 'Marcar como no visitado' : 'Marcar como visitado'}\n              aria-pressed={state.visited}\n            >\n              <Check size={18} strokeWidth={2.5} />\n            </button>\n            {routeActive ? (\n              <button\n                className="icon-button icon-button--route-collapse"\n                onClick={onCollapse}\n                type="button"\n                aria-label="Plegar ficha y mantener la ruta"\n              >\n                <ChevronDown size={18} />\n              </button>\n            ) : null}\n            <button className="icon-button icon-button--ghost" onClick={onClose} type="button" aria-label="Cerrar ficha">\n              <X size={19} />\n            </button>\n          </div>\n          <p>\n            <span className="category-label" style={{ color: category.color }}>{category.label}</span>\n            <span aria-hidden="true"> · </span>\n            {place.locality}\n          </p>\n        </div>\n      </header>`

if (!details.includes(oldHeader)) throw new Error('No se encontró el header esperado en PlaceDetails.tsx')
details = details.replace(oldHeader, newHeader)

const oldCss = `.place-details__header {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto;\n  gap: 8px;\n  align-items: center;\n  margin-top: 2px;\n}\n\n.place-details__title {\n  min-width: 0;\n}\n\n.place-details__header h1 {\n  overflow: hidden;\n  margin-top: 0;\n  font-size: clamp(1.03rem, 4.8vw, 1.3rem);\n  line-height: 1.1;\n  letter-spacing: -0.035em;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n.place-details__header p {\n  margin-top: 2px;\n  color: var(--color-ink-muted);\n  font-size: 0.7rem;\n}\n\n.place-details__header-actions {\n  display: flex;\n  gap: 2px;\n  align-items: center;\n}`

const newCss = `.place-details__header {\n  display: block;\n  margin-top: 2px;\n}\n\n.place-details__title {\n  min-width: 0;\n}\n\n.place-details__header h1 {\n  display: -webkit-box;\n  overflow: hidden;\n  margin-top: 0;\n  font-size: clamp(1.03rem, 4.8vw, 1.3rem);\n  line-height: 1.12;\n  letter-spacing: -0.035em;\n  overflow-wrap: anywhere;\n  -webkit-box-orient: vertical;\n  -webkit-line-clamp: 2;\n}\n\n.place-details__header p {\n  margin-top: 3px;\n  color: var(--color-ink-muted);\n  font-size: 0.7rem;\n}\n\n.place-details__header-actions {\n  display: flex;\n  gap: 2px;\n  align-items: center;\n  margin-top: 5px;\n}`

if (!css.includes(oldCss)) throw new Error('No se encontró el bloque CSS esperado para el header')
css = css.replace(oldCss, newCss)

await writeFile(detailsPath, details)
await writeFile(cssPath, css)
